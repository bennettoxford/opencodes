# Helper function fill usage for missing years

This only fills gaps between existing start and end dates for each code
but does not extent the date range for a code.

## Usage

``` r
complete_usage_gaps_with_zeros(data)
```
