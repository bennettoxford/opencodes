# Dataset choices for the app's dataset radioButtons

Dataset choices for the app's dataset radioButtons

## Usage

``` r
dataset_choices()
```

## Value

A named character vector suitable for
`shiny::radioButtons(choices = ...)`: labels are names, dataset ids are
values
