% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get-data.R
\name{ensure_app_datasets_cached}
\alias{ensure_app_datasets_cached}
\title{Make sure every dataset the Shiny app can show is available before it starts}
\usage{
ensure_app_datasets_cached()
}
\description{
Called once from \code{run_app()}. On the deployed app, every dataset is
already bundled in \verb{inst/app-data/}, so this is a no-op: no downloads, no
parquet reads. In local development it downloads and caches whatever
isn't already there, so picking a dataset from the "Select data" dropdown
never stalls the UI on a first-time download once the app has started.
}
\keyword{internal}
