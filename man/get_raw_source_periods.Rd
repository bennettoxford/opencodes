% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raw-sources.R
\name{get_raw_source_periods}
\alias{get_raw_source_periods}
\title{Get raw NHS Digital source locations for a dataset}
\usage{
get_raw_source_periods(dataset)
}
\arguments{
\item{dataset}{String, dataset name, e.g. "gp_snomed", "hesapc_icd10"}
}
\value{
A named list, one entry per period. For \code{gp_snomed} each entry is a
url string; for the others, a list with \code{url} plus fields like \code{sheet},
\code{skip_rows}, or \code{range}.
}
\description{
Reads \verb{inst/config/raw_<publication>.yml}, named by this dataset's
\code{publication} field in \code{tidy_data_sources.yml}, and returns its \code{periods}
list. To add a new year, add one entry to that file's \verb{periods:} map.
}
\details{
A publication can back more than one dataset (e.g. \code{hesapc_icd10} and
\code{hesapc_icd10_breakdowns} read the same xlsx file, different sheet
region); \code{variant} picks which sub-fields to merge in.
}
\keyword{internal}
