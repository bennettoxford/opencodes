% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raw-sources.R
\name{get_raw_source_periods}
\alias{get_raw_source_periods}
\title{Get raw NHS Digital source locations for a dataset}
\usage{
get_raw_source_periods(dataset)
}
\arguments{
\item{dataset}{String, dataset name, e.g. "snomed_usage", "icd10_usage",
"icd10_usage_breakdowns"}
}
\value{
A named list, one entry per period. For \code{snomed_usage}, each entry
is a single url string. For the others, each entry is a list with \code{url}
plus the fields that dataset's data-raw script needs to read the right
part of the file (e.g. \code{sheet}, \code{skip_rows}, \code{range}).
}
\description{
Reads the matching \verb{inst/config/raw_<publication>.yml} file and returns
its \code{periods} list - the same shape as the hardcoded url lists that used
to live at the top of each \verb{data-raw/*.R} script. To add a new year of
data, add one entry to that file's \verb{periods:} map - the \verb{data-raw/*.R}
scripts don't need to change.
}
\details{
A "publication" is one NHS Digital release (snomed, icd10, opcs4). Some
publications back more than one dataset - icd10_usage and
icd10_usage_breakdowns, for example, both read the same xlsx file per
period, just a different sheet region - so \code{raw_icd10.yml} stores the
shared \code{url}/\code{sheet} once per period, with \code{usage}/\code{breakdowns} sub-fields
for what each dataset reads from it. This function resolves the dataset
name to its publication file and merges in the right sub-fields.
}
\keyword{internal}
