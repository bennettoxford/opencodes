% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get-data.R
\name{get_local_data_path}
\alias{get_local_data_path}
\title{Path to a copy of a dataset shipped inside the app, if there is one}
\usage{
get_local_data_path(dataset, version)
}
\arguments{
\item{dataset}{String, dataset name}

\item{version}{String, dataset version}
}
\value{
Character path, or \code{NULL} if no local copy exists
}
\description{
\verb{just deploy} copies the current released parquet for every dataset into
\verb{inst/app-data/} before deploying the Shiny app, so the hosted app reads
data straight off disk instead of downloading it on every cold start (see
\code{copy_app_data_for_deploy()}). Local installs and package users never have
this directory, so this returns \code{NULL} for them and \code{get_dataset()} falls
back to the normal cache-and-download path.
}
\keyword{internal}
